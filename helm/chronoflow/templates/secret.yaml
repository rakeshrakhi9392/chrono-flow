apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chronoflow.fullname" . }}-secrets
  labels:
    {{- include "chronoflow.labels" . | nindent 4 }}
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: {{ .Values.secrets.datasourcePassword | quote }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: chrono-scheduler-service
  namespace: chronoflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chrono-scheduler-service
  template:
    metadata:
      labels:
        app: chrono-scheduler-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: chrono-scheduler-service
          image: i-ayush-singh/chrono-scheduler-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8082
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 45
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "600m"
              memory: "768Mi"
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: chronoflow-config
                  key: SPRING_KAFKA_BOOTSTRAP_SERVERS
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: chronoflow-config
                  key: SPRING_DATA_REDIS_HOST
            - name: SPRING_DATA_REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: chronoflow-config
                  key: SPRING_DATA_REDIS_PORT
            - name: MANAGEMENT_OTLP_TRACING_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: chronoflow-config
                  key: MANAGEMENT_OTLP_TRACING_ENDPOINT
---
apiVersion: v1
kind: Service
metadata:
  name: chrono-scheduler-service
  namespace: chronoflow
spec:
  selector:
    app: chrono-scheduler-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: chrono-scheduler-service
  namespace: chronoflow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: chrono-scheduler-service
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
